"""Pytest configuration.

Its presence puts the repository root on ``sys.path`` so that tests can import
``morph_scipy``, the SciPy reference implementation that lives outside the
installable package.
"""
