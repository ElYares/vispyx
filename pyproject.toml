[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vispyx"
version = "0.4.0"
description = "Technical image processing package with a morphology core implemented from scratch."
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
authors = [
  { name = "elyarestark" }
]
keywords = ["image-processing", "morphology", "computer-vision", "medical-imaging", "python"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.7",
  "Programming Language :: Python :: 3.8",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Image Processing",
]
dependencies = [
  "opencv-python",
  "numpy",
  "scikit-image",
  "matplotlib",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "scipy",
]
# Backend opcional en Rust. `vispyx` es y sigue siendo un paquete puro de
# Python: este extra solo acelera, nunca hace falta. Todavia no esta publicado
# en PyPI, asi que por ahora se instala desde el repo con
# `cd native && maturin develop --release`; ver native/README.md.
fast = [
  "vispyx-native",
]

[project.scripts]
vispyx = "vispyx.cli:main"

[tool.setuptools]
license-files = ["LICENSE"]

[tool.setuptools.packages.find]
include = ["vispyx*"]
